# tr - translates the whole string from set1 to set2
# tr [set1] [set2]
# -s means to squeeze the following char if there are multiple occurences of it
# tr ' ' '\n' -> this command converts every space into a newline
# tr -s ' ' '\n' 
# https://www.tecmint.com/tr-command-examples-in-linux/
# sort: To make the same string successive so that uniq could count the same string fully and correctly.
# uniq is the tool that helps to detect the adjacent duplicate lines and also deletes the duplicate lines.
# -c -> count : It tells how many times a line was repeated by displaying a number as a prefix with the line.
# sort -r: -r means sorting in descending order
# awk '{ print $2, $1 }': To format the output, see https://linux.cn/article-3945-1.html.
